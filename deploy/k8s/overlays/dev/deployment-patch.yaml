apiVersion: apps/v1
kind: Deployment
metadata:
  name: jaejadle-app
  labels:
    environment: development
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: jaejadle-app
          resources:
            requests:
              memory: "50Mi"
              cpu: "15m"
            limits:
              memory: "150Mi"
              cpu: "50m"
