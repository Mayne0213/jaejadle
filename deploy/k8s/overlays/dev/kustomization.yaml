apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jaejadle-dev

resources:
  - ../../base
  - resourcequota.yaml
  - namespace.yaml
  - sealed-jaejadle-secret.yaml

commonLabels:
  environment: development

# 이미지 태그 설정
images:
  - name: ghcr.io/mayne0213/jaejadle
    newTag: develop-sha-acfdc0f489b7409e749fa79c25e36c32b3c5d60d

patchesStrategicMerge:
  - deployment-patch.yaml
