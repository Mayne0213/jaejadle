apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jaejadle

resources:
  - ../../base
  - resourcequota.yaml
  - externalsecret.yaml
  # - sealed-jaejadle-secret.yaml  # Replaced by ExternalSecret

commonLabels:
  environment: production

# 이미지 태그 설정
images:
  - name: ghcr.io/mayne0213/jaejadle
    newTag: main-sha-eadb42a7b2f6f2fab1768d6a74655c88d4a4b319

patchesStrategicMerge:
  - deployment-patch.yaml
