apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jaejadle

resources:
  - ../../base
  - resourcequota.yaml
  - externalsecret.yaml
  # - sealed-jaejadle-secret.yaml  # Replaced by ExternalSecret

commonLabels:
  environment: production

# 이미지 태그 설정
images:
  - name: ghcr.io/mayne0213/jaejadle
    newTag: main-sha-e3f1e095fb61d20ce49436afa2a346eb68c4efa2

patchesStrategicMerge:
  - deployment-patch.yaml
