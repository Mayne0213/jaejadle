apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jaejadle

resources:
  - ../../base
  - resourcequota.yaml
  - secretstore.yaml
  - externalsecret.yaml
  - ingress.yaml

commonLabels:
  environment: production

# 이미지 태그 설정
images:
  - name: github.com/Mayne0213/jaejadle
    newTag: main-sha-94a2e80cafab356d0d977d3a427b4d3be6d1b05e

patchesStrategicMerge:
  - deployment-patch.yaml
